{
  "name": "youtube-creator-assistant",
  "version": "1.0.0",
  "private": true,
  "description": "CreatorHub - YouTube creator idea management (full-stack monorepo)",
  "scripts": {
    "install:all": "npm install --prefix backend && npm install --prefix frontend",
    "db:push": "npm run db:push --prefix backend",
    "start:backend": "npm run dev --prefix backend",
    "start:frontend": "npm start --prefix frontend",
    "dev": "node scripts/dev.js",
    "build": "npm run build --prefix frontend",
    "seed": "npm run seed --prefix backend",
    "seed:patterns": "npm run seed:patterns --prefix backend",
    "test": "npm test --prefix backend && npm test --prefix frontend",
    "test:backend": "npm test --prefix backend",
    "test:frontend": "npm test --prefix frontend"
  }
}
